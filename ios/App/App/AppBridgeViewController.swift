import Capacitor

class AppBridgeViewController: CAPBridgeViewController {
    // InAppPurchasePlugin 已由 capacitor.config.json packageClassList 自动注册，
    // 勿在此重复 registerPluginInstance，否则会触发 Overriding existing registered plugin。
}
